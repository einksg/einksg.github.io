{
  "name": "eink-sg",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "vite build && node scripts/build-routes.js",
    "preview": "vite preview --host 0.0.0.0",
    "test": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0",
    "vite": "^7.1.0"
  },
  "dependencies": {
    "@fontsource-variable/inter": "^5.3.0"
  }
}
